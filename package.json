{
  "name": "starter-basic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"next dev\"",
    "build": "tinacms build -c \"NODE_ENV=production next build\"",
    "start": "tinacms build -c \"NODE_ENV=production next start\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "next": "^16.2.5",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "tinacms": "^3.7.5"
  },
  "devDependencies": {
    "@tinacms/cli": "^2.4.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.2.5"
  }
}
